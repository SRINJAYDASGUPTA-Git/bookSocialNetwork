spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/book_social_network
    driver-class-name: org.postgresql.Driver
    username: username
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: dasguptasrinjay7@gmail.com
    password: ${SMTP_PASSWORD}
    properties:
        mail:
            smtp:
              auth: true
              starttls:
                enable: true
                trust: "*"
    protocol: smtp
application:
  security:
    jwt:
      secret-key: ${SECRET_KEY}
      expiration: 86400000
  mailing:
    frontend:
      activation-url: http://13.61.27.122:8080/activate-account
  cors:
    origins: http://localhost:8080,http://localhost:8088,http://13.61.27.122:8080,http://13.61.27.122:8088
  file:
    uploads:
      photos-output-path: ./uploads

server:
  port: 8088